package com.sonymobile.cameracommon.research.parameters;

interface ShootingLabel$StringParameter extends ShootingLabel$Parameter {
    boolean equals(String str);

    ShootingLabel$Parameter getDefaultValue();
}
